const model = require("../models").UserEmails;
const bcrypt = require("bcrypt")

const resetPass = async (req, res) => {
  try {
      const               
  } catch (error) {
    console.log(error);
  }
};
  

module.exports={
  resetPass
}